# Core (pin to known-good modern versions)
teachbooks>=0.2.2           # wrapper around jupyter-book
jupyterbook-patches>=1.9.0
matplotlib==3.10.7          # <- force newer Matplotlib
requests                    # ok to leave unpinned

# Sphinx extensions (leave flexible unless you hit a conflict)
sphinx-image-inverter
download_link_replacer
sphinx-exercise
teachbooks-sphinx-tippy
sphinx-iframes
sphinx-named-colors

# Thebe (kept from GitHub as TeachBooks docs suggest)
git+https://github.com/TeachBooks/Sphinx-Thebe
